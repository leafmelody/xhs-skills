name: xhs_get_feeds
description: |
  获取小红书首页推荐列表（For You 页面内容）。

input_schema:
  type: object
  properties:
    limit:
      type: integer
      description: 返回数量限制
      default: 20
      minimum: 1
      maximum: 50

examples:
  - input:
      limit: 10
    description: 获取首页前10条推荐
